const symbol1 = '¥';

function displayPrice(price) {
  var symbol1;
  if (false) {
    let symbol1 = '$';
  } else {
    const symbol2 = '£';
  }

  console.log(symbol1 + price);
  // console.log(symbol2 + price);
}
displayPrice('80');
